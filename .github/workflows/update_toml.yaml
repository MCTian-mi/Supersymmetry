name: Update Toml

on:
  workflow_dispatch

jobs:
  build:
    name: "Update Toml"
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: refresh
      shell: bash
      run: |
        chmod +x packwiz refresh